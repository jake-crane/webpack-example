This project gives a basic demonstration of using webpack.

Requirements:
    Node.js

To run this example, cd to webpack-example, run "npm install" and "./node_modules/.bin/webpack" then open index.html.
